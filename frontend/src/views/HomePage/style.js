export const sxHomePage = {
  header: (theme) => ({
    display: 'flex',
    padding: theme.spacing(2),
    justifyContent: 'space-between'
  }),
  title: (theme) => ({
    textAlign: 'left',
    display: 'flex',
    gap: theme.spacing(2)
  })
};
